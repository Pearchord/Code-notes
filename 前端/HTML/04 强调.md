如斜体 em
```
<p>我很<em>庆幸</em>你没有<em>迟到</em>。</p>
```
粗体 strong
```
<p>这杯液体<strong>毒性很大</strong>。</p>
```
(可嵌套)

 `<b>`、`<i>` 或 `<u>` 表达字面上的粗体、斜体或下划线
 `<strong>`、`<em>`、`<mark>` 或 `<span>` 从语义出发，表达重要、强调和标记。

现在的开发规范是“结构与表现分离”。想让字变粗，应该去 CSS 里写 `font-weight: bold`，而不是直接用 `<b>`。


